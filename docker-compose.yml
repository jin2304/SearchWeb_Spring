version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: searchweb-db
    environment:
      POSTGRES_DB: searchwebdb
      POSTGRES_USER: admin1111
      POSTGRES_PASSWORD: 1111
      TZ: Asia/Seoul
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # - ./src/main/resources/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - searchweb-network
    restart: always

networks:
  searchweb-network:
    driver: bridge

volumes:
  postgres_data:
